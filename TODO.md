# Fix errors task - TODO

- [x] Patch `app.py` to configure Flask template/static lookup to include:
  - `templates/`
  - `smart campus/templates/`
  - `smart-hub/templates/`
  - and corresponding `static/` directories.
- [x] Re-run `python -m py_compile app.py`.
- [x] Restart Flask and verify `/login` loads.


